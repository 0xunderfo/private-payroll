{
  "name": "zk-payroll-backend",
  "version": "0.1.0",
  "description": "Backend for ZK Payroll zero-fee claims",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "circomlibjs": "^0.1.7",
    "hono": "^4.0.0",
    "snarkjs": "^0.7.6",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
